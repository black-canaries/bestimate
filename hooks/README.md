# Hooks

Custom React hooks for shared logic.

## Common Patterns

- `useConvex*` - Hooks that wrap Convex queries/mutations
- `use*Form` - Form handling hooks
- `use*` - General purpose custom hooks

## Guidelines

- Keep hooks focused and reusable
- Document hook parameters and return values
- Use TypeScript for type safety
